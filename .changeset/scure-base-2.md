---
"kheopskit": patch
"@kheopskit/core": patch
"@kheopskit/react": patch
---

Bump `@scure/base` to `^2.2.0`, aligning core with the range the examples already use. The `base58`/`base64` coders used by the address validators and the Solana signer are unchanged; no API or behavior change.
